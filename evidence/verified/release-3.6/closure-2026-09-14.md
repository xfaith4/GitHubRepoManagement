# Release 3.6 — Every Repository Gets an Outcome — closure — 2026-09-14

**Window:** 2026-09-14, D-018 PR 2 (the second of the two PRs D-018 named).
**Source of truth:** the checks named on each milestone, green in CI Smoke on
this PR's head; the same gates pass in the local canonical suite on Windows.
**Verified:** means what §3 of ROADMAP.md says since 2026-09-13 — the milestone's
`check:` exits 0 in CI on the PR head. It does **not** mean operator field proof;
see "Not claimed".

## What closes

Release 3.6 shipped between 2026-08-26 and 2026-08-27 in five pull requests
(#188, #189, #191, #192, #193), each merged on a green CI Smoke. Its six milestones carried
`smoke-tested` for seventeen days while the release sat in `validation`,
waiting on eyes that the new vocabulary no longer lets a milestone wait on. Each
milestone's check, and the gate behind it:

| Milestone | Check | Gate that decides it |
| --- | --- | --- |
| Conclusion model (backend) | `pwsh ./scripts/Invoke-ModuleSmokeTest.ps1` | "Foundation conclusions" section: validator red on a blank reason, no route and bare `L0-Absent` before nine fixtures all conclude and coverage reconciles |
| Outcome card (UI) | `pwsh ./scripts/Invoke-ApiHostSmokeTest.ps1` | conclusions payload shape over the live index; 9 component + 11 data tests in the frontend suite |
| First interaction — the ranked `Today` landing | `pwsh ./scripts/Invoke-ModuleSmokeTest.ps1` | Dashboard source-order tripwire; 13 ranking + 10 view tests; viewMeta contract test |
| Flexible standards | `pwsh ./scripts/Invoke-ModuleSmokeTest.ps1` | a JSON-only detection rule flips a conclusion with no code change; `OutcomeCard` renders `not-applicable` with its reason |
| Measure — coverage and leverage | `pwsh ./scripts/Invoke-ApiHostSmokeTest.ps1` | series shape and range; every metric states a basis; the two uncaptured metrics named and null |
| Define the intentional-engineering evidence model | `pwsh ./scripts/Invoke-ModuleSmokeTest.ps1` | config-integrity gate refuses a scored status on the `not-scored` domain |

## Evidence the release was used as designed

The Release 3.7 conclusion pass on 2026-09-13 ran `Get-RepositoryFoundationConclusion`
over the nine cohort repositories (`evidence/trials/release-3.7/cohort.json`,
index SHA recorded per record): nine conclusions, zero contract violations,
eight naming an action the product performs itself. The same pass exposed the
three false positives now carried as Current focus M4a–M4c — which is the
outcome Release 3.6's own goal describes, a conclusion for every repository,
being measured rather than assumed.

## Not claimed

- **Operator field proof.** Eyes on the `Today` landing, the outcome card and
  the Insights leverage panel are OQ-1 in `docs/governance/operator-queue.md`.
  Recording it (`scripts/Add-OperatorVerification.ps1`) ratchets this archived
  record; nothing waits on it.
- **Kind detection beyond `archived`.** Named in the release as a non-blocker;
  it is Current focus M4a with its own check.
- **The foundation-coverage series' history.** It accrues on the same clock as
  maturity history; the 2.9 trend-accrual item carries it.
